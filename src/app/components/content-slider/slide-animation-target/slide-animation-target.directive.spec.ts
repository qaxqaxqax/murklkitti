import { SlideAnimationTargetDirective } from './slide-animation-target.directive';

describe('SlideAnimationTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideAnimationTargetDirective();
    expect(directive).toBeTruthy();
  });
});
