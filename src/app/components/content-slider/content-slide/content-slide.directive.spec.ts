import { ContentSlideDirective } from './content-slide.directive';

describe('ContentSlideDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentSlideDirective();
    expect(directive).toBeTruthy();
  });
});
