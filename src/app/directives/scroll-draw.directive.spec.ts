import { ScrollDrawDirective } from './scroll-draw.directive';

describe('ScrollDrawDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollDrawDirective();
    expect(directive).toBeTruthy();
  });
});
